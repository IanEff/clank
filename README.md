# clank

TODO: one-line description.


## Run

```sh
make run
```

## Build

```sh
make build
./bin/clank
```

## CI locally

```sh
make ci    # fmt-check → vet → lint → test → build
```

## Security

```sh
make vulncheck    # scan dependencies for known vulnerabilities (govulncheck)
```

